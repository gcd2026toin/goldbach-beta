# ゴールドバッハ - セットアップガイド

## ✅ 何が修正・追加されたか

### 1️⃣ Bot難度持ち越し機能
- **ファイル**: `App.tsx` + `src/screens/HomeScreen.tsx`
- **変更**: ゲームから帰ってもBot難度設定がリセットされなくなった

### 2️⃣ ダークモード対応
- **ファイル**: `app.json`
- **変更**: `userInterfaceStyle: "automatic"` → スマホ設定に自動追従

### 3️⃣ アイコン設定対応
- **ファイル**: `app.json` + `assets/` フォルダ
- **仕様**: あなたが用意した画像を配置するだけで OK

---

## 🚀 使い始める前に

### ステップ 1: あなたが用意した画像を配置
必要なファイル（各 1024×1024 px）:
```
assets/
├── icon.png                        ← あなたの画像に置き換え
├── android-icon-foreground.png     ← あなたの画像に置き換え
├── android-icon-background.png     ← あなたの画像に置き換え
├── android-icon-monochrome.png     ← あなたの画像に置き換え
└── favicon.png                     ← あなたの画像に置き換え（192×192以上）
```

### ステップ 2: ローカルテスト
```bash
cd goldbach-app
npm install
npx expo start

# スマホで Expo Go → QRコード読み込み
# スマホ設定でダークモード ON/OFF → リアルタイムに色が切り替わる
```

### ステップ 3: APK ビルド
```bash
eas build --platform android --profile preview
```

### ステップ 4: スマホにインストール
ビルド完了後、APKをスマホにインストール

---

## 📋 動作確認チェックリスト

### ライトモード（デイライト）
- [ ] 背景: `#FAF6EE` (温かみのある白)
- [ ] テキスト: `#2A2420` (濃いグレー)
- [ ] ボタン: `#B8863B` (ゴールド)
- [ ] トランプ: `#FFFDF8` (温白)

### ダークモード（ナイト）
- [ ] 背景: `#16151A` (シックな黒)
- [ ] テキスト: `#EDE8DE` (明るい灰)
- [ ] ボタン: `#D9AA4D` (明るいゴールド)
- [ ] トランプ: `#FFFDF8` (温白・変わらない)

### Bot難度機能
- [ ] ホーム画面で b1/b2/b3 の難度を変更
- [ ] ゲーム実行
- [ ] 「セット結果」から「ホームへ戻る」
- [ ] ホーム画面で難度がリセットされていない ✅

---

## 📚 詳細ドキュメント

このフォルダに含まれているドキュメント:
- **`ICON_AND_THEME_GUIDE.md`** ← 最初に読むやつ
- **`ICON_SETUP.md`** - アイコン設定の詳細
- **`THEME_COLORS.md`** - ダークモード色の詳細
- **`ASSETS_README.md`** - assets フォルダ構成ガイド

---

## 🎨 簡単なアイコンデザインポイント

### 必須
- **高コントラスト**: 小さく表示されても視認性が高い
- **中央配置**: 上下左右の余白を確保（安全エリア: 中央 ~780×780 px）

### 推奨色（ゴールドバッハテーマ）
```
背景:       #FAF6EE (温かみのある白)
前景（金）: #D9AA4D (ゴールド)
前景（黒）: #2A2420 (濃いグレー)
```

### デザインパターン例
- シンプル G モノグラム（金×黒）
- トランプスート + 金色枠
- 素数イメージ + 金色の光沢

---

## ❓ よくある質問

**Q: ダークモードでアイコンも変わる？**
A: いいえ。アイコンは固定色です（実物のトランプと同じ）

**Q: トランプカードの色が違う？**
A: いいえ。両モード共通 `#FFFDF8` です（意図的な設計）

**Q: カラーをカスタマイズしたい**
A: `src/theme.ts` を修正してください

---

## 📁 プロジェクト構成

```
goldbach-complete/
├── App.tsx                     ← 修正済み（Bot難度保持）
├── app.json                    ← 修正済み（ダークモード対応）
├── src/
│   ├── screens/
│   │   └── HomeScreen.tsx      ← 修正済み（Bot難度保持）
│   ├── ThemeContext.tsx        ← ✅ 既に完璧
│   ├── theme.ts                ← ✅ 既に完璧（ダークモード対応）
│   ├── engine/                 ← ゲームルール・AI
│   ├── components/             ← UI要素
│   ├── trophies/               ← トロフィーシステム
│   └── ...
├── assets/
│   ├── icon.png                ← 置き換え予定
│   ├── android-icon-*.png      ← 置き換え予定
│   └── favicon.png             ← 置き換え予定
├── package.json                ← そのまま使用
└── [ドキュメント各種]
```

---

## 🎯 次のステップ

1. **画像ファイルを配置** ← あなたがやる
2. **`npm install`** (初回のみ)
3. **`npx expo start`** でテスト
4. **ダークモード ON/OFF で確認**
5. **`eas build --platform android`** でビルド
6. **高校文化祭で頒布！** 🎉

---

**最終更新**: 2026年7月29日
**作成者**: Claude
**プロジェクト**: ゴールドバッハ - 高校文化祭用数論トランプゲーム
